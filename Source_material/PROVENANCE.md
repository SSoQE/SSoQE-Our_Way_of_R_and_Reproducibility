# Reproducibility source-material provenance

This review bundle was copied from `SSoQE-Reproducibility_in_Science` at commit `64e29516eecd2f57e188eb0f33dfa5e9c1f0043c`. The restore tag `20260831-before2026` points to that commit, and the source repository remains unchanged.

The bundle includes editable Quarto, YAML, JSON, and R sources; the raw penguin input; the source licence; and explicitly selected referenced assets. Rendered HTML, generated Markdown, generated dependency directories, generated SCSS/font products, `docs/`, and the copied renv environment were intentionally excluded.

`Presentation/Materials/Logos/sprout_logo.png` was also intentionally excluded because it belongs to a separate project and is not part of the SSoQE identity. The legacy QMD still contains its original reference for editorial review, so this source-material copy is not an independent render target.

All copied files are byte-identical to the tagged source and are recorded in [SHA256SUMS](SHA256SUMS). Treat this directory as immutable source material until the editorial consolidation is separately reviewed.
